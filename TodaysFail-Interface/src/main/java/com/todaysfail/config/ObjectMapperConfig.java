package com.todaysfail.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ObjectMapperConfig {
    // @Bean
    // public ObjectMapper objectMapper() {
    // 	return new
    // ObjectMapper().configure(com.fasterxml.jackson.core.JsonParser.Feature.ALLOW_UNQUOTED_CONTROL_CHARS,
    // 			true).registerModule(new JavaTimeModule());
    // }
}
